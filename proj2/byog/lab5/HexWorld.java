package byog.lab5;
import org.junit.Test;
import static org.junit.Assert.*;

import byog.TileEngine.TERenderer;
import byog.TileEngine.TETile;
import byog.TileEngine.Tileset;

import java.util.Random;

/**
 * Draws a world consisting of hexagonal regions.
 */
public class HexWorld {
   /** private static final int WIDTH = 60;
    private static final int HEIGHT = 30;

    public void addHexagon (TETile[][] world,int size, int x, int y, ){
        int colume = size + 2 * (size - 1);
        for (int i = 0; i < size; i += 1) {
            for (int j = 0; j < (colume - (size + i * 2))/2; j += 1) {
                world[x + j][y + i] = Tileset.NOTHING;
            }
            for (int j = 0; j < size + i * 2; j += 1) {

            }
        }
        for (int i = 0; i < 4; i += 1) {

        }
    }

    private int indent (int size, int line){

    }*/
}
